<div class="bg-slate-800 max-h-full w-1/4 left-0 text-3xl font-bold">
  <nav class="py-12 px-4">
    <ul>
      <li class="mb-4"><%= link_to "New Post", new_post_path if user_signed_in? %></li>
      <li class="mb-4"><%= link_to "Home", root_path %></li>
      <li class="mb-4"><%= link_to "My Profile", profile_path(current_user) if user_signed_in? %></li>
      <li class="bottom-4 absolute"><%= link_to "About Us", about_path %></li>
    </ul>
  </nav>
</div>
